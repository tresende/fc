apiVersion: v1
kind: Service
metadata:
  name: goserver
spec:
  selector:
    app: goserver
  type: ClusterIP
  ports:
  - port: 9001 # service port
    protocol: TCP
    name: http-goserver
    targetPort: 8080 # container port
## kubectl apply -f k8s/k8s/deployment.yaml && kubectl port-forward svc/goserver 8080:9001

  # type: NodePort
  # ports:
  # - port: 8080 # service port
  #   nodePort: 30001 # service port
  #   protocol: TCP
  #   name: http-goserver
  #   targetPort: 8080 # container port


## kubectl apply -f k8s/k8s/deployment.yaml && kubectl port-forward svc/goserver 30001:8080


  # type: LoadBalancer
  # ports:
  # - port: 9001 # service port
  #   protocol: TCP
  #   name: http-goserver
  #   targetPort: 8080 # container port